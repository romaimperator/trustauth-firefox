Foamicate
=========

This is a collection of parts that combine together to make a system that allows authentication of users without using passwords but instead using public key cryptography.

Parts
-----

Foamicator - This is the Firefox plugin to support authentication and key generation for Foamicate.
Foamicatee-PHP - This is the simple PHP script to authenticate users.
